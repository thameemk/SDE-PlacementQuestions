#include <iostream>
#include <bits/stdc++.h>

using namespace std;

int length_of_last_word(string s)
{
    // split to list of words and return the length of last word if it is not " " else return the length of second last element
}

int main()
{
    string s = "Hw The";
    int response = length_of_last_word(s);
    cout << "\n"
         << response << "\n";
    return 0;
}